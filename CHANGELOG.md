# 更新记录

## 0.3.4 - 2026-08-08

- 修复：托盘计时器分支曾错误地截获主图标双击事件，导致“已收纳窗口”面板无法打开；现已修正消息路由。
- 新增：托盘主图标提示中显示版本号，便于确认实际运行的版本。
- 新增：窗口消息处理异常写入 `%APPDATA%\TrayPocket\error.log`，便于后续定位问题。
- 测试：增加主图标双击打开面板的回归测试。

## 0.3.3 - 2026-08-08

- 修复：托管窗口被程序自行重新显示时，TrayPocket 会重新隐藏并保持状态一致。
- 修复：程序重建主窗口句柄时复用原托管项，避免同一真实窗口出现多个条目。
- 修复：通过替代窗口恢复后统一销毁托管项，避免残留不可用图标。
- 新增：双击 TrayPocket 主图标打开“已收纳窗口”面板，可查看状态并恢复或移除项目。
- 调整：默认只显示一个 TrayPocket 主托盘图标；可在设置中重新开启每个项目的独立图标。
- 测试：新增窗口自动显示、句柄重建、恢复清理和单主图标模式回归测试。

## 0.3.1 - 2026-07-06

- 新增：`build-python-exe.ps1`，使用 PyInstaller 将 Python 版打包为独立 Windows exe。
- 发布：提供 `TrayPocket-python-v0.3.1.exe`，用户无需安装 Python 即可运行。

## 0.3.0 - 2026-07-06

- 新增：Python 版实现 `src/traypocket.py`，使用标准库 `ctypes` 调用 Windows API。
- 新增：`run-python.ps1`，可直接启动 Python 版 TrayPocket。
- 新增：`build-python-package.ps1`，用于生成 Python 版发布压缩包。
- 调整：README 将 Python 版作为当前主线实现说明，保留 C# 版作为早期实现。
- 新增：托盘菜单增加“设置”子菜单。
- 新增：可在设置里开启或关闭开机自动启动。
- 新增：可设置隐藏窗口时是否播放系统提示音，配置保存到 `%APPDATA%\TrayPocket\settings.txt`。
- 新增：设置菜单增加“菜单语言”，支持选择中文（简体）菜单。

## 0.1.0 - 2026-07-05

首个可用版本。

- 新增：选择 `.exe` 后直接启动并隐藏到系统托盘。
- 新增：`Win + Shift + Z` 隐藏当前前台窗口。
- 新增：双击托盘小图标恢复被隐藏窗口。
- 新增：最近程序列表，支持一键再次托盘运行。
- 新增：单实例命名管道转发，避免重复启动多个 TrayPocket。
- 新增：后台进程托管，没有主窗口的程序也能显示为可管理项目。
- 新增：开机自动启动开关。
- 新增：中文界面、中文注释、中文 README。
- 新增：MIT 开源许可证和第三方来源说明。
